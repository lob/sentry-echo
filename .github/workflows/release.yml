name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: [self-hosted, lob-runner, x64, Linux]
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          release-type: go
